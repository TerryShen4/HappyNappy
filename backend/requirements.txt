# HappyNappy backend + dashboard dependencies
# Install into a virtualenv:  pip install -r requirements.txt
# Versions pinned to what the project was developed/tested against (Python 3.14, Windows).

# --- API server (api_server.py): FastAPI + Bluetooth-serial ingest + heart-rate processing ---
fastapi==0.128.4
pyserial==3.5
uvicorn==0.40.0
heartpy==1.2.7
numpy==2.4.2
scipy==1.17.0

# --- Streamlit dashboard (streamlit_dashboard.py) ---
streamlit==1.58.0
requests==2.34.2
pandas==3.0.3
plotly==6.7.0
